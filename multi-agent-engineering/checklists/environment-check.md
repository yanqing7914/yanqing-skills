# Environment Check

Before multi-agent work, capture:

- Current workspace / cwd
- Git status and existing user changes
- Repo instructions that apply
- Test or build commands available
- Whether native subagents exist in this client

If writes or tests are blocked, downgrade the plan or ask the user.
